<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Getgrub</title>

  <!-- jQuery -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <!-- Materialize -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
  <!-- Materialize Icon -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.1/css/all.min.css"
    integrity="sha512-xA6Hp6oezhjd6LiLZynuukm80f8BoZ3OpcEYaqKoCV3HKQDrYjDE1Gu8ocxgxoXmwmSzM4iqPvCsOkQNiu41GA=="
    crossorigin="anonymous" />
  <!-- CSS -->
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>

  {{{ body }}}

  <script src="https://cdnjs.cloudflare.com/ajax/libs/ckeditor/4.14.1/ckeditor.js" integrity="sha512-P4rrEImABBYtBFdmrFPqF8rjh8iiRiupfQ4KLrBCL8dAivLiM2nh+1bJQeMb2ZglmIlGZdfTtgGdXH6H/hGVeg==" crossorigin="anonymous"></script>
  <script src="/js/search-handlers.js"></script>
  <script src="/js/script.js"></script>
  <script>
    CKEDITOR.replace('comments0', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments1', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments2', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments3', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments4', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments5', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments6', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments7', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments8', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments9', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments10', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments11', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments12', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments13', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments14', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
    CKEDITOR.replace('comments15', {
      plugins: 'wysiwygarea, toolbar, basicstyles, link'
    })
  </script>
</body>

</html>